import React from 'react';
import { TitleContainer, TitleText } from './Title.styles';

const Title: React.FC = () => {
  return (
    <TitleContainer>
      <TitleText>in the spotlight</TitleText>
    </TitleContainer>
  );
};

export default Title;
